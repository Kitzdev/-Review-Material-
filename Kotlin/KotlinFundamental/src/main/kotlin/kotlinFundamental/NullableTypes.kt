package kotlinFundamental

fun main() {
    /*
        We can allow a variable to be intialized by null by adding ? to the
        variable types
     */

    val text: String? = null
    //val length: Int = text.length --> compile time error
}