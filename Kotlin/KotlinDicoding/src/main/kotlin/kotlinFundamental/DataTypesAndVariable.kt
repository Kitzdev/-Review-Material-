package kotlinFundamental

fun main()
{
    //[var/val] [identifier]: [type] = [initialization]

    /*
        The difference between var and val is, var will create a variable that
        it's value can be changed over the runtime. While val will create a
        variable that it's value will cannot be changed once it has been
        initialized with a value
     */

    val name: String = "Kitbert"
}
